{
    "devDependencies": {
        "husky": "^9.1.7",
        "lint-staged": "^16.1.2",
        "prettier": "^3.6.2",
        "prettier-plugin-sh": "^0.18.0"
    },
    "scripts": {
        "prepare": "husky"
    },
    "lint-staged": {
        "*.{yaml,yml,json,js,md}": "prettier --write",
        "{Dockerfile,*/Dockerfile}": "prettier --write"
    }
}
